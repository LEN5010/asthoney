from pathlib import Path
import json,re
OUT=Path(__file__).resolve().parents[2]/'docs/final';TITLE='基于多智能体的生成式欺骗蜜罐网与态势感知系统'
reports={
'132-刘毅凡':[
('主要工作与工程取舍','我负责项目的总体设计、核心模块实现和联调，前端重构与最终交付也由我统筹。这个项目不是把几个模型接到页面上，而是把诱捕入口、会话状态、意图判断、存储和展示组织成一条能回看的链路。外层 SSH 使用真实握手并免登录凭据，进入后的命令则限制在仿真主机世界里。这样既保留了客户端交互体验，又避免把攻击载荷变成宿主机上的真实命令。报告中的模型、规则与模拟处置需要分开说明，不能依靠“智能体”这一名称掩盖实际实现。'),
('设计中的主要问题','连续交互比单次返回更难处理。攻击者在一台主机上创建文件，再进入另一台主机，回来后应当仍能找到此前的修改。如果只维护当前主机的一份状态，横向移动会覆盖原有世界。本项目以会话为单位保存多主机快照，把当前资产与访问过的主机分开管理。读取凭证只增加证据，不直接触发换主机；内层 ssh 才改变活动主机。这些边界让行为能够被测试，也便于解释拓扑变化。'),
('程序调试与验证思路','本轮验收暴露了两个容易被演示成功掩盖的问题。一个是首页把多个请求放在同一个失败分支里，告警接口短暂异常就会使拓扑也不更新；另一个是启动脚本先调用系统 Python，再调用虚拟环境中的服务程序，造成环境检查与实际启动不一致。解决思路分别是按面板隔离错误，以及在启动前确定唯一解释器。对问题的判断不能只看页面是否打开，还要检查错误出现后其他区域是否继续工作、修复后是否存在可重复的验证步骤。'),
('界面与项目结果分析','旧界面把事件、统计、拓扑和说明放进相似卡片，所有区域都在争夺注意力。最终版本以拓扑和会话为中心，指标压缩成摘要条，回放详情改成标签切换，清理入口放入维护区。浅色样式只是表面变化，更重要的是操作顺序清楚、错误有解释、数据来源可追溯。最终代码通过九十三项 Python 测试；独立 Agent 完成三十四条命令、三台主机的探索并返回，网页可回放六十八条输入输出事件。这个结果说明已覆盖场景能够复现，并不等于系统具备生产环境的容量和安全保证。'),
('课程收获与后续改进','我在项目中更重视状态边界与交付证据，而不是继续增加功能名称。生成式输出需要可靠的确定性分支兜底，图数据库中的关联关系需要与页面显示一致，运行文档中的命令需要在干净目录执行。后续改进应优先完善虚拟命令语义、线索之间的一致性和更细粒度的技术映射，并为实时事件增加持久化机制。团队协作方面，应当把界面校验、异常测试和材料复核分成可验证的小任务，让每个成员根据自己的完成记录写报告，而不是在最后阶段用同一份总结替换姓名。')],
'133-蔡铭达':[
('参与范围与核验说明','分配给我的收尾任务是五个页面在两种桌面尺寸下的检查、中文文案校对和界面问题复核，不承担核心后端或前端重构代码的作者责任。本稿根据项目代码和现有验收记录整理，具体操作与完成情况须由我按任务清单复核后确认。检查范围包括总体态势、会话审计、终端回放、攻击矩阵和来源画像，分辨率分别为一千三百六十六乘七百六十八与一千九百二十乘一千零八十。'),
('界面检查的方法','界面检查不能只判断颜色是否好看。我需要先按教师观看演示的顺序观察首页，确认拓扑和决策步骤是否在首屏可见，再查看事件更新是否抢走阅读位置。表格应能看清会话、来源和操作入口，长命令不能把页面撑宽。对于矩阵，需要分清观测次数和风险等级；同样的蓝色深浅表示频次，而严重告警才使用红色。文案中的“模拟隔离”和“启发式研判”必须保留限定，避免让界面表达超过实际功能。'),
('具体问题与复核案例','项目验收记录中的一个具体案例是回放页信息过多：终端旁边同时堆叠研判、主机信息、决策和意图，阅读者需要持续向下寻找内容。当前方案用四个标签保留全部信息，同时让终端保持主要位置。我需要验证标签切换后播放进度是否保留，左右方向键能否移动到相邻标签，当前选中状态是否清楚。仅检查鼠标点击成功还不够，重新加载页面、长文本和没有会话参数时的状态都可能暴露遗漏。'),
('对调试与结果的理解','前端调试要区分布局、数据和网络三个层面。比如没有显示告警，可能是接口请求失败，也可能是确实没有记录，二者不能都显示成零。现有总览对接口失败添加独立错误条，其他面板保留可用数据；复核时应主动模拟一次失败，并在恢复后确认错误条消失。浏览器验收脚本已覆盖两种尺寸和多种操作，但自动检查的通过不能代替逐页看图。尤其是文字虽然没有溢出，仍可能因为字号小或信息过密而难以在投影中阅读。'),
('收获与后续工作','这份任务让我明确了界面质量的评价维度：信息先后顺序、操作状态、文本可读性和功能表述的准确性，比装饰更直接影响演示。后续本人核验应当记录页面名称、分辨率、复现步骤、实际表现与预期表现，避免只写“这里不美观”。对可重复的问题先保留截图，再交给组长修改，复测时使用同一条件。个人报告应据实际复核补充本人的发现，不能把自动化脚本已有的运行记录直接写成自己的手工操作经历。')],
'134-王子越':[
('任务范围与记录依据','分配给我的工作是运行已有测试和真实 SSH 冒烟、整理正常与异常场景，并核对测试结论。本稿以当前代码及项目验收日志为依据，尚需本人执行清单并确认实际完成情况。我不把核心诱捕代码的实现记为个人贡献，也不把已有自动化结果当成本人独立发现的问题。测试的目的不是获得一串通过数字，而是用可复现的输入检查系统是否按照设计处理状态变化、保持目录、文件与主机状态连续，并保留能够解释结果的证据。'),
('测试用例的组织思路','我需要按输入边界、会话行为和展示结果组织用例。输入边界包含空命令、未知命令和带控制字符的文本；会话行为包含免凭据 SSH、同连接跨通道、主机切换和文件快照；展示结果包含告警、模拟隔离、回放和页面错误状态。每条记录应写明准备条件、命令或操作、预期输出、实际输出及结论。同一来源连续快速发送测试命令可能触发自动化特征，所以测试数据与普通交互应当分开，不能把测试造成的高风险画像解释成真实攻击样本。'),
('具体调试案例','项目收尾修正了一个有代表性的测试错误：旧冒烟脚本向2222端口直接发送 TCP 字符串，并期待得到 whoami 的响应。但该端口已经升级为真实 SSH，必须先完成协议握手，直接发送文本不能证明 shell 消毒有效。当前脚本改用 AsyncSSH 连接测试服务，通过 exec 通道发送带 ANSI 控制序列的命令，再检查输出中是否出现预期用户以及是否仍有控制字符。另一个细节是交互提示符可能没有末尾空格，测试不应把某一种提示符字符串当作协议保证。'),
('结果分析与边界','最终验收包含九十三项 Python 用例；Grok 四点七的独立探索记录有三十四条目标命令、三台虚拟主机，且逐层返回入口。Python 测试既包含纯逻辑，也包含本机临时端口上的真实 SSH，因此在禁止监听的受限环境中失败时，需要先判断是否为环境限制，而不是立即修改业务代码。本轮没有重新验证全新容器安装，不能直接沿用旧版本的容器结论。外部模型的短句连通性检查只说明当时能得到响应，不证明长时间稳定性、生成内容质量或课堂网络始终可用。'),
('课程收获与复核要求','我对测试工作的理解是，断言必须和要证明的功能位于同一层。页面截图不能替代真实协议测试，HTTP 成功也不能证明终端世界保存正确。本人后续执行时应保留完整日志和异常说明，既记录成功，也记录未覆盖条件；测试失败后先缩小输入，再定位模块，最后复跑相关与全量用例。项目当前仍缺少长期压力和全部浏览器组合测试，这些限制应保留在结果分析中。只有把实际执行条件写清楚，其他成员或教师才能按相同步骤复现，而不是只看到未经解释的通过结论。')],
'218-江博琛':[
('负责范围与个人核验','分配给我的收尾任务是核对图表编号、文献引用、运行说明和最终提交目录，不负责系统核心模块的编码。本稿根据现有材料结构及项目交付要求整理，具体核对工作需本人执行后确认。文档整理并不是把内容复制进 Word，而是检查文档是否能够准确说明最终版本，以及文件能否被另一位读者找到和使用。团队报告与个人报告的命名、成员顺序、学号后三位和组号均应保持一致。'),
('材料一致性的检查方法','我需要先对照最终代码和截图检查报告中的能力描述。例如早期文稿把入口写成纯 SSH 仿真，而当前外层握手已经使用真实协议，只有登录后的主机世界和内层横向仍然仿真。再如页面已经取消主题切换，报告就不应继续介绍深浅主题按钮。图题放在图片下方，表题放在表格上方，编号需要与正文引用一致。参考文献不仅要有格式，还应能对应正文确实引用的框架或规范，不能为了增加数量列出没有使用的材料。'),
('具体问题与解决思路','现有材料中第六部分设计和第九部分总结曾单独保存在 Markdown，而 Word 仍是启动阶段版本。如果只更新封面日期，正文仍会缺章并保留旧测试结论。当前收尾方案把章节合并成完整团队报告，并重新使用最终工作台截图。本人核验时应逐节检查“已实现”和“计划实现”的时态，特别是免凭据 SSH、连接复用、模型降级和模拟隔离的边界。对于个人报告，责任分配不能自动变成已完成事实，成员应根据各自实际操作确认文字。'),
('运行说明与交付检查','运行说明也是可执行材料。应当把压缩包解压到新目录，再按文档生成配置、启动数据库与应用、打开页面并执行演示。源代码目录不能携带真实环境配置、生成私钥、虚拟环境和缓存，否则既增加包体积，也会把开发机的状态误当成运行条件。默认模型配置为空，启动脚本生成数据库配置，不要求管理令牌或登录私钥；第一次下载镜像需要联网，而部署完成后的默认演示不依赖外部模型。这样的前提需要明确写出，才能解释为什么不同计算机可能出现不同安装时间。'),
('对调试与课程结果的认识','文档问题也可以用程序化方式检查，例如统计个人报告字数、核对文件数量、扫描包内敏感路径，再逐页查看 PDF 是否存在图片裁切、表格断行或标题孤立。程序检查能减少遗漏，但不能判断每段文字是否准确表达了本人经历，所以人工确认仍不可省略。当前项目形成了代码、测试和报告相互对应的交付结构，后续维护时应在界面或接口变化后同步更新截图与说明。对我而言，这项任务的学习重点是把“文件已经生成”与“材料可被复现和理解”区分开来。')]
}
for author,sections in reports.items():
 blocks=[]
 for i,(title,text) in enumerate(sections,1):blocks += [{'type':'h','level':1,'text':f'{i} {title}'},{'type':'p','text':text}]
 data={'title':TITLE,'kind':'个人课程设计报告' if author.startswith('132') else '个人课程设计报告 本人核验稿','author':f'第14组  {author}','cover':False,'filename':f'{TITLE}（{author}）.docx','blocks':blocks}
 (OUT/(author+'.json')).write_text(json.dumps(data,ensure_ascii=False,indent=2))
 md='# '+TITLE+'\n\n'+data['kind']+'\n\n'+data['author']+'\n\n'+'\n\n'.join('## '+str(i)+' '+a+'\n\n'+t for i,(a,t) in enumerate(sections,1))
 (OUT/(author+'.md')).write_text(md)
 count=len(re.findall(r'[\u4e00-\u9fff]', ''.join(t for _,t in sections)))
 print(author,count,'汉字')
 assert count>=800
