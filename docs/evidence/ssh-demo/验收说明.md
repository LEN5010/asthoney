# 本机 SSH 演示版最终验收

集中修改后统一验收，不在实现过程中逐处重跑测试。

- Python 回归：73 项通过，记录见 pytest.txt。
- 真实 SSH：10 项通过，记录见 ssh-results.json；无密码或客户端密钥，多 exec 共用连接状态，跨主机返回、新连接隔离、WebSocket 和模型研判通过。
- 浏览器第一阶段：9 项功能通过，包括真实 SSH 实时终端、手选会话保持、断线重连、局部失败恢复、搜索、回放、矩阵和画像。
- 第一轮发现1366尺寸决策步骤超出首屏。仅调整终端高度、卡片间距和去掉装饰性步骤编号后，继续复测布局及剩余场景，14 项通过；见 ui/results.json 和各页面截图。没有重复运行后端回归或 SSH 验收。
- 指南：Word/PDF 共3页，逐页检查后替换 Downloads 中的旧指南。

界面测试复用了工作区已安装的 Playwright。干净目录执行 npm ci 和 npx playwright install chromium 后可运行 npm run test:ui。当前正式报告与旧提交 ZIP 不在本次三页指南更新范围，提交前须按新实现同步。
