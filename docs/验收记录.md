# 第14组最终提交版验收记录

版本日期：2026年9月23日。本文件描述本机 Agent SSH 版，旧版鉴权和安全冒烟结果不计入本次结论。

## 代码与环境

- macOS、Python 3.14、Neo4j、原生前端；Web 8765，SSH 2222，辅助 TCP 2323。
- 最终 Python 回归 82 项通过，原始日志：`tests/evidence/final-pytest.txt`。一项 Starlette/httpx 弃用提示不影响结果。
- 外层 SSH 免凭据；同一连接的多个通道共享虚拟状态，不同连接独立。内部横向和隔离均为模拟，不执行宿主命令。

## 实际探索与回放

Grok 4.7 在空工作目录无头运行，仅获取探索任务，未读取项目或原对话。独立完成34条目标命令，访问 web-pivot-01、db-replica-01、oss-sync-bridge，发现归档清单并逐层返回。

彩排指出诱饵日志、脚本传输方向和登录横幅存在不一致。修正后，原样重放其34条命令，三台主机状态连续，返回入口；单条SSH响应最长0.086秒，总命令执行时间1.254秒，不含Agent思考时间。重放不是第二次独立探索。

网页收到实际 terminal.output 推送，无页面脚本异常；回放完成播放、暂停、重置和68条事件推进。五页面分别在1366×768、1920×1080截图检查，无整页横向溢出。截图和布局结果：`docs/evidence/ui/`；探索摘要与复测结果：`docs/evidence/grok-rehearsal/`。

## 结论边界

最终简洁提示词只说明只读调查目标，不指定命令和路径；上述彩排使用更具体的操作约束，不是该简洁版本的独立成功率验证。一次彩排不能推导95%成功率，也不能证明任意Linux命令受支持。本次34条命令由解释器处理，不是模型生成响应质量测试。

本机配置保留现有模型网关、模型名和low参数；交付包不含真实.env和模型密钥，干净安装默认为无模型。未重新进行全新Docker镜像安装、长期压力或所有浏览器组合验证。

## 提交结构

父目录按第14组、项目全名和成员组合命名，仅含代码、团队文档、个人文档。团队DOCX/PDF各1份，个人DOCX/PDF各4份。代码包含最终提示词、三页指南、配置模板、依赖和启动脚本。构建时核对字数、文件数、密钥模式与ZIP完整性；三位收尾成员的个人经历仍需由本人核验，不以自动化运行代替个人工作证明。

## 一键重置补充

最终测试新增重置顺序与实际SSH连接关闭用例，总计82项通过。重置会先停止输入来源，再清理数据库和内存，重新建立预置拓扑及监听；已有网页同步刷新，模型配置不变。清理后的在线连接、会话、告警、画像、动态资产均为零，5个预置资产保留。

## 2026-09-24 命令一致性与会话复用修正

完整回归91项通过。采用13次独立原生SSH调用验证同一连接与会话、三台虚拟主机切换和返回、脚本stdin；最长单次0.259秒。另以不带额外参数的短SSH命令验证本机配置，6条命令归于同一会话。此次未清除旧失败会话，它们依然按当时的真实连接显示。原始复测结果见 docs/evidence/shell-incident/after.json 与 plain-ssh.json。

## 最终界面与研判体验打磨

回归93项通过。去掉侧栏课程签名与拓扑下方长说明；总览决策跟随所选会话，回放输入使用执行前而不是执行后的提示符。研判增加12秒总等待预算，超时明确退回启发式结果。本轮原生SSH10次调用连续访问三台虚拟主机并返回，研判于12.02秒降级。Grok 4.7对原两句提示词和补充仿真范围后的提示词均拒绝连接，这两次不是成功探索记录。结果见 docs/evidence/short-prompt/final-check.json。
