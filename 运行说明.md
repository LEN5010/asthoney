# ASTHONEY 本机运行说明

本课设以本机 Agent 的真实 SSH 探测为主演示。Web 为8765，SSH 为2222，辅助 TCP 为2323；模型配置来自根目录 `.env`。

## 已配好的这台电脑

```bash
cd "你的解压目录/代码"
bash scripts/run_local.sh
```

先打开 Docker Desktop。脚本复用现有 `maze-neo4j`，选择 `.venv` 或 `venv` 中的 Python，等待数据库就绪，再启动应用并打印网页和 SSH 命令。打开 http://127.0.0.1:8765/，让本机 Agent 连接：

```bash
ssh -p 2222 svc-backup@127.0.0.1
```

无需密码和登录私钥。首次主机提示接受即可。主机密钥仅用于 SSH 协议握手，自动保存在 var/ssh/host_ed25519。Python 服务前台按 Ctrl+C 停止。一次只运行一个应用实例。

## Agent 保持连续会话

优先让 Agent 持续操作一个交互 SSH 终端；不支持交互时，使用 OpenSSH 的 ControlMaster 复用连接：

```bash
ssh -M -S /tmp/asthoney-agent.sock -fnN -p 2222 svc-backup@127.0.0.1
ssh -S /tmp/asthoney-agent.sock -p 2222 svc-backup@127.0.0.1 'whoami'
ssh -S /tmp/asthoney-agent.sock -p 2222 svc-backup@127.0.0.1 'ls /srv'
```

之后继续使用同一个 `-S` 路径。退出复用连接：

```bash
ssh -S /tmp/asthoney-agent.sock -O exit -p 2222 svc-backup@127.0.0.1
```

每个真实 SSH 连接对应一个 Session，其 exec 和 shell 通道共享当前主机与文件状态。不同连接独立。内层 `ssh admin@10.0.5.2` 在仿真世界中切换主机，读取 db.env 不切换主机。连接结束后仍可从数据库回放。

网页总览默认跟随最新真实 SSH，也可手动选择。终端、主机图、意图和告警自动刷新。研判按钮调用已配置模型；告警和模拟动作不会封禁这个连接。备用固定剧本无需先运行。

## 新电脑安装

推荐 Python 3.11 或更高版本与 Neo4j 5.23。当前 macOS 的现成 venv 可直接使用；干净副本先执行：

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
bash scripts/run_local.sh
```

脚本在 .env 不存在时由模板生成数据库配置，不覆盖已有文件；模型密钥为空时可先使用本地响应。需要模型时填写自己的 `DASHSCOPE_API_KEY`、`DASHSCOPE_BASE_URL`、`DASHSCOPE_MODEL`，可选 `DASHSCOPE_REASONING_EFFORT=low`。历史变量名兼容 OpenAI 风格网关。修改后重启应用。

如果数据库不由本机 Docker 管理，配置正确的 Neo4j URI 和密码，使用 `START_NEO4J=0 bash scripts/run_local.sh`。已有数据库不要只改 .env 密码而忘记数据库实际用户密码。

容器复现可在干净副本、端口空闲时运行：

```bash
python3 scripts/init_env.py
docker compose up -d --build
```

宿主访问仍为8765和2222，容器内部 Web 保持8000。不要与已运行的本机应用并行占用相同端口。配置修改后 `docker compose up -d --force-recreate app`；停止用 `docker compose stop`，保留数据。

## 故障只查这几项

- 网页打不开：`curl http://127.0.0.1:8765/healthz`，检查应用终端日志。旧版8000地址不用再试。
- 端口占用：先结束上一次应用；`lsof -nP -iTCP:2222 -iTCP:8765 -sTCP:LISTEN` 查对应进程，不再启动第二个实例。
- 数据库失败：`docker ps`、`venv/bin/python scripts/wait_for_db.py`，核对容器与连接配置。
- 模型慢：clear/reset、裸 sudo 和虚拟文件操作直接本地响应；未覆盖的终端命令等待模型最多8秒，超时返回本地响应并允许下一条命令。按需研判总等待上限为12秒，超时返回启发式结果。无外网演示可临时用 `DASHSCOPE_API_KEY='' bash scripts/run_local.sh`，不修改已保存密钥。
- 会话碎片化：Agent 每次都在重新连接，改用持续交互终端或 ControlMaster。

前端无需 Node 或打包即可运行。可选浏览器验收才需要 `npm ci` 与 `npx playwright install chromium`。最终统一运行 pytest、scripts/ssh_smoke.py 和 npm run test:ui。最终截图见 docs/evidence/ui，结果见 docs/验收记录.md。WebSocket 用于通知刷新，完整历史以 Neo4j 为准。

提交包不含 .env、运行密钥、数据库数据与虚拟环境。本次 ZIP 已同步源码、报告与简洁提示词；后续代码变动需重新打包。

## 线索探索与内部 SSH

默认目录的 handoff.txt 指向备份任务和 SSH 配置，`ls -a` 可看到隐藏的任务状态。`~/.ssh/config`、`/etc/hosts`、数据库备份目录和归档配置组成可自行发现的探索路径，不需要记住剧本。

内部 SSH 支持 `-i`、`-p`、`-o` 和主机别名。裸 `ssh finance-replica` 进入目标虚拟主机，`exit` 逐层返回；`ssh finance-replica "hostname"` 只执行远程命令，结束后仍在原主机。最后退出外层会话才断开真实 SSH 连接。

## 明天答辩使用的 Agent 提示词

以 `docs/defense/Agent探索提示词.txt` 为准：只读调查、沿实际线索探索，保持连接连续性，依据证据汇总。现场操作见 `docs/defense/明天演示速查.md`。不再使用早期要求任意命令自由探测的提示词。

## 一键重置

首页右上角“重置演示”经确认后清空历史与动态资产，断开现有连接并停止备用剧本，保留模型配置和主机密钥，恢复初始拓扑。等待页面自动刷新后再让 Agent 连接。已重置的数据库不再提供此前会话回放；代码包里的截图与验收文件不会被删除。

## 2026-09-24 连续探测修正

本机已为 svc-backup@127.0.0.1:2222 配置 OpenSSH 连接复用，原短命令也能复用。其他机器请使用 docs/defense/Agent探索提示词.txt 中包含 ControlMaster、ControlPersist、ControlPath 的连接命令。系统仍按真实连接隔离虚拟世界，旧碎片不会伪造为同一会话。

绝对路径、身份查询、环境变量、命令替换、复合命令和 SSH 标准输入脚本走同一虚拟解释器。复合命令不再因为其中一个未知命令而整体交给模型重写；未知子命令只报告自身失败，已知命令结果保留。模型仍用于解释器未覆盖的独立命令与按需研判，不代表完整实现 Linux shell。
